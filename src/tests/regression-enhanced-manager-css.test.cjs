/* Deprecated regression test stub for old category manager CSS. */

module.exports = {};

if (typeof describe !== 'undefined') {
  describe('Category Manager CSS regression (legacy)', () => {
    test('no-op', () => expect(true).toBe(true));
  });
}
